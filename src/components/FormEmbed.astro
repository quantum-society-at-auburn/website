---
import type { CollectionEntry } from 'astro:content';

interface Props {
  entry: CollectionEntry<'forms'>;
}

const { entry } = Astro.props;
---

<iframe src={entry.data.googleFormUrl} width="100%" height="900" loading="lazy">
  Loading…
</iframe>
